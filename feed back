当日いただいたフィードバック（要改修）
課題3, 5の実行がLLMの能力的にうまくいかない

• gpt-4o-mini だとかなり改変が必要(課題5ならdescriptionだけでなく関数名等も変更が必要).
• 性能が低すぎて、多少の改変に気付くことすらできない.
• gpt-5 だと少しの改変(テキスト通りの改変)で上手くいくらしい.

課題6の実行が想定通りに進まない

• JSONファイルをわざと壊す等が必要.
• `read_tickets()`を存在しないチケットが与えられたらエラーが起きるプログラムにすると、多分想定通りになる.
• `return func(**kwargs)`内でエラーが起きる必要がある.
• `assert false`を最終行に追加することで簡単に実現可能.

課題7で変化がわからない | 以下変更案

• tanakaさんのpasswordを`reset_pending`以外にする or `reset_passwords()`の中の初期パスワードを変える.
• `reset_passwords()`の引数で新しいパスワードを指定できるようにする.
